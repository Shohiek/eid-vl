CREATE TABLE  IF NOT EXISTS `hivpc_data_export` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `lab_no` varchar(50) NOT NULL,
  `lab_name` varchar(50) NOT NULL,
  `location` varchar(25) NOT NULL,
  `ward` varchar(25) NOT NULL,
  `patient_id` varchar(50) NOT NULL,
  `national_id_num` varchar(50) NOT NULL,
  `age` int(11) NOT NULL,
  `days` int(11) NOT NULL,
  `sex` varchar(10) NOT NULL,
  `ref_no` varchar(50) NOT NULL,
  `specimen_collection_date` timestamp NOT NULL,
  `specimen_registration_date` timestamp NOT NULL,
  `results_first_printed_date` timestamp NOT NULL,
  `specimen_type` varchar(50) NOT NULL,
  `icd10` varchar(50) NOT NULL,
  `notes` varchar(50) NOT NULL,
  `reviewed_by` varchar(50) NOT NULL,
  `reviewed_date` timestamp NOT NULL,
  `entered_by` varchar(50) NOT NULL,
  `entered_date` timestamp NOT NULL,
  `hivpc` varchar(50) NOT NULL,
  `hivpc_odrl` varchar(50) NOT NULL,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=latin1 COMMENT='Raw data supplied'