angular
.module('dashboard')
.controller('dashboardCtrl',['$scope','$timeout','ngProgress',function($scope,$timeout,ngProgress){

	// ngProgress.color('#cfba13');
	// ngProgress.start();
	// $timeout(ngProgress.complete(), 100000);


}])
